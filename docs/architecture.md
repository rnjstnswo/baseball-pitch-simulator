# Architecture Overview

> Stub — to be completed in Phase 6.

This document will contain a full system architecture diagram (data flow, component diagram, deployment topology) once the system is built.

## Planned Sections

- Data pipeline diagram (pybaseball → Parquet → feature pipeline → model artifacts)
- API service architecture (FastAPI, Pydantic, SHAP)
- Frontend component tree
- Deployment topology (Render/Fly.io + Vercel + GitHub Actions)
